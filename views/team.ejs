<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Team</title>
</head>
<body>
	<%= team.name %> <br>
	<br>
	<ul>
		<% for( var i = 0; i < players.length; i++){ %>
			<li><a href="/player/<%= players[i]._id %>"><%= players[i].number %>: <%= players[i].name %></a></li>
		<% } %>
	</ul>
	<a href="/">back</a>
	<form action='/team/<%= team._id %>/player' method='POST'>
		<select name="player" id="">
		<% for( var i = 0; i < all_players.length; i++){ %>
			<option value="<%= all_players[i]._id %>"><%= all_players[i].number %>: <%= all_players[i].name %></option>
		<% } %>
		</select>
		<button>save</button>
	</form>
</body>
</html>